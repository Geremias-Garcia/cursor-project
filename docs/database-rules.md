# Database Rules

- Use UUIDs for public entities.
- Never expose sequential IDs publicly.
- Use timestamps everywhere.
- Use soft deletes when appropriate.
- Use foreign keys internally.
- Bid records must be immutable.
- Use transactions for all bidding operations.